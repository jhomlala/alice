name: alice_example
description: "Example project to showcase Alice."
publish_to: 'none'
resolution: workspace

version: 1.0.0+3

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  material_ui: ^1.2.0
  flutter:
    sdk: flutter
  path_provider:
  alice:
    workspace: true

dev_dependencies:
  build_runner:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
