name: example
description: "A project to showcase http client and Alice integration."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  alice:
  alice_http_client:

dev_dependencies:
  flutter_lints: ^4.0.0

dependency_overrides:
  alice:
    path: ../../packages/alice
  alice_http_client:
    path: ../../packages/alice_http_client

flutter:
  uses-material-design: true
