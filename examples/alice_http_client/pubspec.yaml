name: alice_http_client_example
description: "A project to showcase http client and Alice integration."
publish_to: 'none'
resolution: workspace

version: 1.0.0+3

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  material_ui: ^1.2.0
  flutter:
    sdk: flutter
  alice:
    workspace: true
  alice_http_client:
    workspace: true

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
