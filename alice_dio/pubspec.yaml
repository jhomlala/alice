name: alice_dio
description: "Dio plugin for Alice."
version: 1.0.0
homepage:

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  alice:
    path:
      ../alice
  dio: ^5.4.3+1

dev_dependencies:
  very_good_analysis: ^5.1.0