name: example
description: "A project to showcase dio and alice integration."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  alice:
    path: ../../alice
  alice_dio:
    path: ../../alice_dio


flutter:
  uses-material-design: true
