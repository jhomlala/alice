# Changelog Rules

These are the rules for managing `CHANGELOG.md`:

1.  **Never remove previous changelog**: All historical entries must be preserved.
2.  **Use an "Unreleased" section**: All new changes should be added to an "Unreleased" section at the top of the file before they are associated with a specific release version.
